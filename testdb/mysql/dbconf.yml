development:
  driver: mysql
  open: root:tc-demo@tcp(localhost:3306)/tstore_dev?parseTime=true

test:
  driver: mysql
  open: root:tc-demo@tcp(localhost:3306)/tstore_test?parseTime=true

staging:
  driver: mysql
  open: root:tc-demo@tcp(localhost:3306)/tstore_staging?parseTime=true

production:
  driver: mysql
  open: root:tc-demo@tcp(localhost:3306)/tstore_production?parseTime=true
